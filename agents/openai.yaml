interface:
  display_name: "FPGA Cowork"
  short_description: "Initialize FPGA environments and develop approved features."
  default_prompt: "Use $fpga-cowork to map a new board's FPGA hardware environment, then prepare an approval-ready feature plan."
