# D — 只读诊断

D 用于解释已有源文件、仿真日志、构建报告、时序报告或既有 ILA 数据，不执行新的
仿真、构建、下载或硬件采集。

开始前明确待回答的问题和最少需要阅读的证据。结论写入工作包 `out/diagnostics/` 或
`AI-work/diagnostics/`，包含所查原始路径、关键证据、结论级别（已证实、可能或无法
确定）、影响范围和最小下一步。

D 不修改工程或原生产物，也不把原生日志、WDB、WCFG、bitstream 或 `.ila` 复制进
AI-work。诊断不能证明尚未执行的修复有效。
